The C Python "test_ext.so" library is compiled automatically
if it is not yet present and the "use_test.py" is run.

To avoid compilation at runtime, or to recompile the library,
execute the "setup.py" script. 
